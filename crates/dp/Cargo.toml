[package]
name = "novachannel-dp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Differential-privacy-calibrated dummy traffic scheduling for metadata protection."

[lints]
workspace = true

[dependencies]
rand = "0.8"

[dev-dependencies]
rand_chacha = "0.3"
