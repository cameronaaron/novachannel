[package]
name = "novachannel-dp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Probabilistic dummy traffic scheduling and size padding; positive epsilon is not a differential-privacy guarantee."

[lints]
workspace = true

[dependencies]
rand = "0.10"

[dev-dependencies]
rand_chacha = "0.10"
