[package]
name = "novachannel-oram"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Path ORAM: an O(log n)-per-access oblivious storage layer for server-side per-user state."

[lints]
workspace = true

[dependencies]
rand = "0.10"
sha2 = "0.11"

[dev-dependencies]
rand_chacha = "0.10"
